# Same-origin production (backend serves frontend/dist): leave empty so API uses `/api`.
# Split deploy (Vercel SPA + Render API): set full URL, e.g.
# VITE_API_URL=https://YOUR-BACKEND.onrender.com/api
VITE_API_URL=

# Optional — Stripe publishable key
VITE_STRIPE_PUBLISHABLE_KEY=
