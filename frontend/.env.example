# Optional. Leave unset when the API is served from the same origin
# (production: Express serves frontend/dist and /api together).
# For a separate API host, set e.g. VITE_API_URL=https://api.example.com/api
# VITE_API_URL=
